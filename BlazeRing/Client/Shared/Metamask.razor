@inject EthereumService EthereumService

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            var accountAddress = await EthereumService.GetAccountAddress();
            // do something with accountAddress...
        }
    }
}